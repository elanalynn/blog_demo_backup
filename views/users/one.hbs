<div class="container container-custom">
  <img class="user-image circle" src="{{user.image_url}}" alt="{{user.first_name}} + {{user.last_name}}" />
  <h2>{{user.first_name}} {{user.last_name}}</h2>
  <p>{{user.bio}}</p>
</div>
